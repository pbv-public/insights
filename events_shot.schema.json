{
  "$id": "/events/shot",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "errors": {
      "additionalProperties": false,
      "description": "Grouping for detected errors resulting from this shot. Omitted if there are no discernible errors.",
      "properties": {
        "faults": {
          "additionalProperties": false,
          "description": "A fault is a type of error, that violates the rules of the game, thus ending the rally. Omitted if there were no faults detected. If this field is present but `is_last_shot` is false, it means play continued despite the fault (i.e., either players overlooked a fault or the AI detected a fault incorrectly).",
          "properties": {
            "excess_bounce": {
              "description": "`true` if the ball bounced more than once before being hit, `false` if the ball only bounced once, omitted if unknown.",
              "type": "boolean"
            },
            "kitchen": {
              "description": "`true` if player stepped in the kitchen while volleying, `false` if the did not, omitted if unknown.",
              "type": "boolean"
            },
            "net": {
              "description": "`true` if the ball was stopped by the net, `false` if the ball did clear the net (even if it touched), omitted if unknown.",
              "type": "boolean"
            },
            "out": {
              "additionalProperties": false,
              "description": "Set if the ball was *traveling out* or *landed out*. Omitted otherwise.\n\n- if this is present but `is_last_shot` is false, it means your opponent hit your ball even though it was going out\n- if `is_last_shot` is true, it means the rally ended because your shot was out",
              "properties": {
                "far_side": {
                  "description": "true if ball was going out to the hitter's far sideline, otherwise omitted",
                  "type": "boolean"
                },
                "left": {
                  "description": "true if ball was going out to the hitter's left, otherwise omitted",
                  "type": "boolean"
                },
                "long": {
                  "description": "true if ball was going out long, otherwise omitted",
                  "type": "boolean"
                },
                "near_side": {
                  "description": "true if ball going was out to the hitter's near sideline, otherwise omitted",
                  "type": "boolean"
                },
                "right": {
                  "description": "true if ball was going out to the hitter's right, otherwise omitted",
                  "type": "boolean"
                }
              },
              "required": [
                "left",
                "right",
                "near_side",
                "far_side",
                "long"
              ],
              "type": "object"
            },
            "paddle_hit_net": {
              "description": "`true` if paddle hit the net, `false if the paddle did not hit the net`, omitted if unknown.",
              "type": "boolean"
            }
          },
          "required": [
            "paddle_hit_net",
            "kitchen",
            "net",
            "excess_bounce"
          ],
          "type": "object"
        },
        "is_dead_dink": {
          "description": "`true` if the `shot_type` is a dink and the ball's resulting bounce ends high enough to be considered easy to attack. If the opposing player hits a ball that would have bounced above the top of the net, this field will be set to `false`.",
          "type": "boolean"
        },
        "is_popup": {
          "description": "`true` if the following conditions are met:\n\n- This shot is hit near the kitchen line or while the player is standing in the kitchen.\n- The ball is hit in an upward trajectory, rising from below the net to above the net.\n- The ball does not bounce on the ground before being hit by the opposing player.\n\nElse, the field is `false`.",
          "type": "boolean"
        }
      },
      "required": [
        "is_dead_dink",
        "is_popup"
      ],
      "type": "object"
    },
    "is_final": {
      "description": "Whether this is the final shot in the rally.",
      "type": "boolean"
    },
    "is_passing": {
      "description": "Whether this shot passed the *nearest* opponent without them touching it. Omitted if unknown.",
      "type": "boolean"
    },
    "is_poach": {
      "description": "Whether this ball was hit closer to the non-hitter's position than the hitter's position along the width of the court, and in front of the non-hitter.\n\nThe players' positions are measured at the time the previous shot was hit,\nand the ball's position is determined at the time the ball was hit by the receiving team.\n\nA poach is only possible if the ball is hit in front of the player the ball was poached from (their teammate).\n\nIf the ball goes past the non-hitter and their partner hits it, it is not considered a poach,\nregardless of the x-axis differential between the players and the ball.\n\nOmitted if unknown.",
      "type": "boolean"
    },
    "is_reset": {
      "description": "Whether this shot takes significant pace off the ball. Omitted if unknown or not applicable.",
      "type": "boolean"
    },
    "is_speedup": {
      "description": "To be considered a speedup, the following conditions must be met:\n\n- the shot adds significant pace on the ball\n- the hitting player is near the kitchen\n- at least one of the opposing players is near the kitchen\n\nOmitted if unknown or not applicable.",
      "type": "boolean"
    },
    "is_volley": {
      "description": "Whether this shot was a volley hit out of the air or not. Omitted if unknown.",
      "type": "boolean"
    },
    "player_id": {
      "$ref": "/types/player_id",
      "description": "The ID of the player who hit the ball.",
      "title": "Player ID"
    },
    "quality": {
      "additionalProperties": false,
      "description": "an evaluation of the quality of the shot",
      "properties": {
        "execution": {
          "$ref": "/types/quality_score",
          "description": "how well the shot was executed",
          "title": "Quality Score"
        },
        "overall": {
          "$ref": "/types/quality_score",
          "description": "the combination of the execution and selection qualities",
          "title": "Quality Score"
        },
        "selection": {
          "$ref": "/types/quality_score",
          "description": "how good of a choice it was to hit this shot",
          "title": "Quality Score"
        }
      },
      "type": "object"
    },
    "resulting_ball_movement": {
      "additionalProperties": false,
      "description": "Stats related to how the ball moves as a result of being hit. Omitted if we couldn't track the ball after the shot was made.",
      "properties": {
        "angles": {
          "additionalProperties": false,
          "properties": {
            "direction": {
              "description": "Direction that the ball was hit from and to.",
              "enum": [
                "MidCrossLeft",
                "MidCrossRight",
                "DownTheMiddle",
                "LeftToMiddle",
                "LeftCrossRight",
                "DownTheLineLeft",
                "RightToMiddle",
                "RightCrossLeft",
                "DownTheLineRight"
              ],
              "type": "string"
            },
            "pitch": {
              "description": "The pitch angle, in degrees within a range of [-90, 90].\n\n- 0 degrees: straight shot (parallel to the ground)\n- 90 degrees: shot directly upwards (perpendicular to the ground)\n- -90 degrees: shot directly downwards (perpendicular to the ground)",
              "maximum": 90,
              "minimum": -90,
              "title": "Pitch Angle",
              "type": "number"
            },
            "yaw": {
              "description": "The yaw angle, in degrees within a range of (-180, 180].\n\n- 0 degrees: shot towards the left sideline (parallel to player's own baseline)\n- 90 degrees: shot towards the net directly (perpendicular to player's own baseline)\n- 180 degrees: shot towards the right sideline (parallel to player's own baseline)\n- Negative values are the same, but in the opposite direction (backwards).",
              "maximum": 180,
              "minimum": -180,
              "title": "Yaw Angle",
              "type": "number"
            }
          },
          "required": [
            "yaw",
            "pitch",
            "direction"
          ],
          "type": "object"
        },
        "crossed_net": {
          "description": "`true` if the ball crosses the net plane, else `false`. Note that this value will be `true` for ATPs (they cross the net plane without the ball necessarily ever going higher than the net).",
          "type": "boolean"
        },
        "distance": {
          "description": "The distance the ball traveled (feet) before coming in contact with the net, court, paddle, etc.",
          "type": "number"
        },
        "distance_from_baseline": {
          "description": "The distance the ball landed from the opponent's baseline.",
          "type": "number"
        },
        "height_over_net": {
          "description": "The height the ball in feet when it _reaches_ the plane of the net. This is\nomitted if the ball never reaches the net plane. A ball that reaches the net but\ndoes not go over will have this value set to the height of the ball when it\nreaches the net (usually this will be greater than 0 but lower than the net\nheight). Similarly, a ball that grazes the net cord and topples over will have\nits height over the net as roughly the height of the net at that location.\n\nNote that the plane of the net exists at `y = 22`.",
          "type": "number"
        },
        "is_volleyed": {
          "description": "Whether the ball was hit out of the air or not.",
          "type": "boolean"
        },
        "speed": {
          "description": "Speed of the ball in MPH.",
          "type": "number"
        },
        "spin": {
          "additionalProperties": false,
          "description": "The spin of the ball after it was hit. Omitted if unknown.",
          "properties": {
            "dominant_class": {
              "description": "The dominant spin class of the ball.",
              "enum": [
                "topspin",
                "backspin",
                "sidespin",
                "minimal_spin"
              ],
              "type": "string"
            },
            "magnitude": {
              "description": "The magnitude of the spin in RPM.",
              "type": "number"
            }
          },
          "required": [
            "dominant_class",
            "magnitude"
          ],
          "type": "object"
        },
        "trajectory": {
          "additionalProperties": false,
          "description": "Describes the estimated flight path of a ball after it was hit by the player.\n\nThe ball essentially flies in a 2D projectile motion along a 2D plane. One dimension\nof this plane is aligned with the Z-axis. An estimation of its path through 3D space\ncan be reconstructed from its initial flight characteristics and start and end\npoints.",
          "properties": {
            "confidence": {
              "$ref": "/types/confidence",
              "description": "Our confidence in this trajectory (based on our confidence in the start and end positions of the ball).",
              "title": "Prediction Confidence"
            },
            "end": {
              "additionalProperties": false,
              "description": "the end of the ball's flight which happens when it bounces, hits the net, is hit by a paddle, or flies out of the video frame",
              "properties": {
                "location": {
                  "$ref": "/types/world_position/3d",
                  "description": "Location of the ball when it ended its flight.",
                  "title": "World XYZ"
                },
                "ms": {
                  "$ref": "/types/video_time",
                  "description": "Number of milliseconds into the video when the ball ended its flight.",
                  "title": "Video Milliseconds"
                },
                "zone": {
                  "$ref": "/events/zone",
                  "description": "Zone where the ball ended its flight.",
                  "title": "Zone"
                }
              },
              "required": [
                "zone",
                "location",
                "ms"
              ],
              "type": "object"
            },
            "peak": {
              "$ref": "/types/world_position/3d",
              "description": "Location where the ball reached its highest (z) point.",
              "title": "World XYZ"
            },
            "start": {
              "additionalProperties": false,
              "description": "the moment the ball starts flying after being hit",
              "properties": {
                "location": {
                  "$ref": "/types/world_position/3d",
                  "description": "Location where the ball was hit.",
                  "title": "World XYZ"
                },
                "ms": {
                  "$ref": "/types/video_time",
                  "description": "Number of milliseconds into the video when the ball was hit.",
                  "title": "Video Milliseconds"
                },
                "zone": {
                  "$ref": "/events/zone",
                  "description": "Court zone that the ball was hit from",
                  "title": "Zone"
                }
              },
              "required": [
                "zone",
                "location",
                "ms"
              ],
              "type": "object"
            }
          },
          "required": [
            "confidence",
            "start",
            "peak",
            "end"
          ],
          "title": "Ball Flight Trajectory",
          "type": "object"
        }
      },
      "required": [
        "angles",
        "crossed_net",
        "is_volleyed",
        "distance",
        "speed",
        "trajectory"
      ],
      "title": "Resulting Ball Movement",
      "type": "object"
    },
    "shot_type": {
      "description": "- smash: an overhead or similar shot where the ball goes from high to low (no upward motion)\n- lob: a shot with a relatively high peak\n- dink: a shot that would land in or near the kitchen and which was hit from near the kitchen\n- drop: a shot that would land in or near the kitchen and was hit from the transition zone or deeper\n- drive: a shot that would land deep if not stopped by an opponent's paddle, opponent's body, or the net (and doesn't fall into an earlier categorization)\n- atp: an around-the-post shot, where the ball is hit outside the sideline and around the net post instead of over the net\n- erne: a shot hit from outside the sideline by the kitchen (based on when the player's feet first touch after a volley)\n- tweener: a shot where the ball is hit as it passes through the hitter's own legs\n\nOmitted if we can't confidently determine a shot type based on the ball's trajectory and other characteristics.",
      "enum": [
        "smash",
        "lob",
        "dink",
        "drop",
        "drive",
        "atp",
        "erne",
        "tweener"
      ],
      "type": "string"
    },
    "stroke_side": {
      "description": "The side of the body the stroke was made on. Omitted if unknown.",
      "enum": [
        "left",
        "right"
      ],
      "type": "string"
    },
    "stroke_type": {
      "description": "The type of stroke used to hit the ball. Omitted if unknown.",
      "enum": [
        "forehand",
        "backhand"
      ],
      "type": "string"
    },
    "vertical_type": {
      "description": "Whether the shot was hit as a dig, overhead, or neutral. Omitted if unknown.",
      "enum": [
        "dig",
        "overhead",
        "neutral"
      ],
      "type": "string"
    },
    "winner_type": {
      "description": "Whether the shot is a clean winner or forced an opponent fault. Omitted if this shot is not a winning shot.",
      "enum": [
        "clean",
        "forced_fault"
      ],
      "type": "string"
    }
  },
  "required": [
    "player_id",
    "quality",
    "is_final"
  ],
  "title": "Shot",
  "type": "object"
}